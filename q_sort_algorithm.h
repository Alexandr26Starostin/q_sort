#ifndef Q_SORT_ALGORITHM_H
#define Q_SORT_ALGORITHM_H

int qsort (void* array, const size_t len_array, const size_t len_type, int (*compare_element) (void* first_element, void* second_element));

#endif
